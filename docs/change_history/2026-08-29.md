# 2026-08-29 后端结构维护

## 卡片外框与左侧菜单交互

- 修复文献阅读异步页面空白：当前 `reka-ui` 仅导出 `DropdownMenuRoot`，页面改为将其别名为模板使用的 `DropdownMenu`，并增加导出契约回归检查。
- 图书馆卡片改为与工作区大卡片一致的 4px 外框并移除细线边框；保留原动效，悬停外框跟随用户柔和主题色。
- Dashboard、任务队列、组件库侧边栏与文献阅读侧边栏的 4px 外框容器移除内部 1px 细线边框。
- 左侧图标栏的“库”“娱乐功能”“我的”二级菜单改为仅点击触发，移除悬停打开/离开关闭逻辑与悬停桥接区域。
- ActivityBar 单文件测试 12 项通过；图书馆、Dashboard 和点击菜单完成截图验证。任务队列、组件库、文献阅读受当前异步页面空白及既有 `reka-ui` 导出错误阻断，未能完成截图验收。

## 变更摘要

- 修复联合搜索默认 limit 为 `None` 时命中结果触发 500，以及异步 profile 切换隐私作用域后文件树和新建草稿被隐藏的问题。
- 将根入口的配置、模型、Service、gRPC 和生命周期装配拆入 `core/bootstrap/` 与 `core/lifespan.py`。
- 新增 `ApplicationServices` 请求级容器；REST 通过 `app.state.services`、FastAPI Depends 和 ContextVar 获取依赖，移除零散 Service 全局变量。
- 新增统一数据库 engine/session factory 和 Alembic 0001/0002 migration；Service 不再建表、自建 engine 或执行 `ALTER TABLE`。
- 将 AgentCore 拆为 graph、stream、session、attachment、child-agent、cancellation、model、token、error-recovery runtime；AgentCore 保留兼容门面。
- 将知识库拆为文件树、入库、预览、搜索和回收站模块；将知识图谱拆为重建、抽取、repository、去重和查询模块。
- 将 123 个 gRPC RPC 拆为 12 个领域 handler，并拆出错误/依赖与响应 mapper。
- 将 builtin 工具迁入领域 package，并把 `services/` 根级 32 个生产模块全部归入领域目录。
- 补充低内存契约/分文件测试工具、迁移清单、验收基线和 README 后端目录说明。
- 按用户要求在 `开发规范.md` 增加内存安全红线：禁止直接全量测试和测试/构建并行。
- 将仅针对仓库根目录的 `.gitignore` 规则加上根锚点，避免 `runtime/` 误忽略 `agent_service/agent_core/runtime/` 源码。
- 按当前仓库重建 README 项目树：列全主要源码、领域、资源、测试和文档目录，仅展开入口、装配、迁移、注册及集中配置单文件。

## 数据库说明

- 实际 runtime 数据库已在 `runtime/backups/db-migrations/agent_service-pre-alembic-repair-20260829.db.bak` 备份。
- 数据库已升级到 `20260829_0002 (head)`，`PRAGMA integrity_check` 返回 `ok`。

## 验证

- OpenAPI、gRPC descriptor 和 Agent stream 契约哈希保持基线一致。
- Alembic 空库、旧库、重复升级和 downgrade/upgrade 测试通过。
- 各阶段按模块执行的 REST、gRPC、Agent、知识库、图谱、builtin 和数据库测试通过。
- 根据用户最终指示，未执行实际界面冒烟和后续批量回归。

## 明暗主题开关迁移

- 移除 TopCommandBar 原有日夜滑块，将统一的圆形物理按键放到左侧活动栏顶部并继续调用 SettingsStore 的持久化主题切换；设置外观页不显示该快捷按钮。
- 公共按键外框保持 36×36，实体按键面由 27px 放大到 34px；浅色和暗色状态统一使用黑色按键面与黄色图标，并缩短按压位移、减弱内凹阴影。
- 浅色状态的静态太阳替换为缩放后的暖色 SVG mask 流体动画；每个按钮实例使用独立 mask ID，并为 reduced-motion 偏好停用形变旋转。
- 暗色状态去除文字与静态月亮，改为缩放后的紫蓝/红紫双层流体球；使用 transform 旋转与 opacity 交替复现示例的颜色变化。

## Dashboard 响应式布局

- 修复中等宽度下 AgenticRAG 指标错误换成两列、上排卡片被压缩以及活跃热力图覆盖 Token 图的问题；平板宽度保持 AgenticRAG 与 Token 并排，下半区按单列自然增长。
- Dashboard 改为统一纵向滚动容器，移动端面板宽度受可用空间约束，可完整滚动到耗时卡片底部且不再产生横向溢出。
- 补充真实耗时明细场景：平板和移动端取消总览、耗时卡内部的嵌套纵向滚动及固定高度，让完整明细随卡片增高并统一由 Dashboard 外层滚动。
- 移动端热力图筛选和指标改为多行网格，平板单列下限制文件类型饼图高度，避免筛选项截断和图表异常放大。
- Playwright 在 1000×900、768×1024、390×844 三档实际界面验证通过；Dashboard 相关 2 个单测文件共 3 项测试通过。

## 模型按需生命周期与进度横幅

- 将模型磁盘验证、断点恢复和内存加载移出 FastAPI 启动生命周期；前端工作区显示后才触发各受管本地模型的独立后台任务。
- 新增用户级 `model_auto_download_enabled` 持久化字段、Alembic `20260829_0003` 迁移，以及 REST/gRPC 对齐的模型偏好、初始化、确认下载和删除接口。
- Embedding 与 ReRank 在工作区启动后异步预热；PaddleOCR 仅在 OCR 已启用或刚启用时加载；当时的内置本地对话模型只在远程大模型不可用且真实 AI 调用发生时验证加载。
- 缺失模型默认等待用户确认；自动下载开启后才后台下载。用户主动删除后以进程内抑制状态阻止本次启动重新下载，下次启动自然恢复策略。
- 新增四模型独立进度横幅与真实字节进度；默认仅在 GitHub 按钮左侧显示 PixelLoader，点击后在 TopCommandBar 下方展开横幅，并在 5 秒后再次收起。Loader 与 GitHub 保留间距，GitHub 展开态预留外描边空间和层级，避免左侧 4px 外框被遮挡。
- 将智能表格表项的五段 PixelLoader 提取为公共组件，供智能表格、模型横幅和 TopCommandBar 复用。
- 修复共享 Embedding、ReRank、PaddleOCR 与旧本地对话模型已加载缓存命中后状态停留在 `loading` 的问题：缓存已就绪时立即将全局模型状态收敛到 `ready`。
- 彻底修复 Embedding/ReRank 偶发永久 loading：两条加载线程原先会并发改写全局 `torch.nn.Module._apply`，可能形成错误的嵌套补丁链。现统一通过带进程锁的 `torch_loading` 入口串行执行全局补丁区并在 `finally` 恢复原函数；provider 支持失效线程重启，管理状态轮询会按真实内存缓存自愈为 `ready`。
- 上述并发修复仅以假 PyTorch、假 provider 和状态机执行聚焦回归：覆盖全局补丁串行与恢复、失效线程重启、缓存命中 READY 自愈；未下载或加载真实模型。
- 修复智能表格结构化填充错误回退内置本地模型：`StructuredGenerationService` 现在注入正式 `SettingsService`，按请求 `user_id` 读取已保存的大/小模型配置后再进入统一调度器。
- 在用户要求停止验证前，已完成拆分的后端生命周期、偏好持久化、模型协调、迁移、REST/gRPC 与前端 API/组件测试；未下载任何真实模型。此后按用户要求未再运行测试或构建。

## Agent 图片附件后台解析与识图开关

- 将会话附件上传拆为“立即落盘并创建 queued 记录”与独立后台解析线程；HTTP 上传不再等待 OCR 或当时的本地视觉推理，多个文件可并发上传且不遮挡 Agent 页面。
- 附件记录元数据新增上传、OCR、识图、收尾、完成和失败里程碑；REST/gRPC 提供单附件状态查询，前端只在对应附件块显示真实上传百分比和解析阶段进度。
- 新增默认关闭的用户级 `vision_understanding_enabled`，通过 Alembic `20260829_0004`、SettingsService、REST/gRPC 和基础设置“识图”开关正式持久化。
- 识图关闭时图片附件仅保留 OCR，`understand_image` 工具会在访问视觉服务前返回关闭提示，禁止隐式加载当时的内置本地模型。
- 修复存储管理模型接口 500/Failed to fetch：恢复 Embedding/ReRank singleton provider 工厂中错位的锁内创建逻辑；OCR 状态查询只选择所需列，避免热重载数据库缺少无关新列时拖垮模型管理；前端对瞬时断连自动轮询恢复。
- 验证均为临时文件、stub、mock XHR 和状态 DTO；未下载或加载真实模型。覆盖后台非阻塞、OCR-only、识图门控、迁移、REST/gRPC、前端请求、文件块进度、模型管理重试及桌面/移动界面冒烟。
- 修复助手回复中上传图片文件名链接打开错误图片：此前 `session-upload://` 被前端误送入知识库 basename 回退，可能命中其他会话或知识库中的同名文件。现 REST/gRPC 均按完整附件 URI 返回原始字节，Markdown 图片链接直接打开精确附件；新消息默认持久化附件 citation，旧消息可从本轮用户附件恢复 URI；同轮重名附件保持非链接状态，禁止静默猜测。



- [x] 维护后端的屎山代码(+readme表示在readme的文件树里面补充),禁止修改任何业务逻辑,仅转移和规范和适配代码.务必遵循`开发规范.md`:
  - `agent_core.py`是上帝类,拆解:
    - `agent_service/agent_core/runtime/`:    
      - 图运行、流式事件转换、会话、附件、子 Agent、取消、模型调用、token 统计和错误恢复,每种分别创建一个文件.
  - `main.py`拆解:  (以下全都+readme)
    - `agent_service/core/bootstrap/`: 
      - `services_bootstrap.py` 服务创建和相互连接    
      - `grpc_bootstrap.py` 启动grpc    
      - `config_bootstrap.py` 加载配置
      - `models_bootstrap.py` 加载和下载模型
    - `agent_service/core/lifespan.py` 生命周期
  - `agent_service/api/rest/deps.py`里面定义了大量 `_xxx_service` 全局变量，再由 `main.py` 逐个赋值.
    - 新建 ApplicationServices 容器，由 bootstrap 创建并保存到 app.state.services；REST 使用 FastAPI Depends 获取依赖。AgentConfig 只管理静态配置，不持有任何 service 实例。
  - `knowledge_library_service.py` 知识库业务太大.
    - 彻底拆解,拆到`agent_service/services/knowledge_library/`里面.原服务文件删除.   
      - 文件树,入库,预览,搜索,回收站,其他操作,分别写一个文件.
  - `knowledge_graph_service.py` 知识图谱业务太大.
    - 彻底拆解,拆到`agent_service/services/knowledge_graph/`里面.原服务文件删除.   
      - 按照图谱的构成过程拆.
  - 多达十几个 service 自行调用 `SQLModel.metadata.create_all()`,而且数据库没有一个真正的统一迁移器.
    - 新建`agent_service/core/db/engine.py`: 统一的数据库factory.    +readme
    - 引入Alembic,建立带版本号的migration,新建`agent_service/core/db/migration.py`,集中处理数据库版本迁移,只在应用启动前统一迁移一次.
    - 将所有service里面越权的`create_all()`去掉,接收 session/repository，不自行建表或建 engine,将现有所有 `ALTER TABLE` 转成幂等迁移版本。
  - `agent_service/core/agent_config.py`暂时不要变
  - `agent_service/tools/`里面的builtin太散乱.
    - 把所有的builtin全都弄到`agent_service/tools/builtin/`里面,按照系统规定的职责划分文件.一类工具一个文件.只移动builtin.
    - `builtin.py`仅保留加载工具的核心逻辑.
  - service层重构: 现有的service层太混杂,单文件和领域文件夹混在一起.
    - 将所有的单文件都归类放在领域文件夹里面,允许一个文件夹只放一个文件.(我就要这么做!)
  - `agent_service/api/grpc/servicer.py`太大,并且业务内容过多.
    - 业务内容全都拆到`agent_service/api/grpc/handlers/`里面去,按照业务领域划分文件     +readme
    - 共享业务逻辑进入`agent_service/services/use_cases/`或者现有的领域service
    - 新增`agent_service/api/grpc/mappers/`处理错误和响应     +readme
    - 本体只承担注册和转换和派发职责.
    - servicer之间相互解耦,REST 和 gRPC 只做 DTO 转换、调用 use case、错误映射。
- [x] 将所有模型的验证下载和加载弄成可选项,以前端节点为条件进行按需加载,不再阻碍在启动过程中间.
  - 当软件完全启动成功之后才验证所有模型的下载
  - 在存储管理的顶部加上以下的用户设置:
    - 启用自动下载=false,启用后,验证为未下载的模型将自动开始下载.(如果用户主动删除,则本次无需自动下载,下次启动时再自动下载)
  - 将四大模型的启用链路从启动过程中间移出去,只有当开始使用和各自模型相关的领域的时候才进行异步多线程的懒加载,不可阻塞任何的其他业务.用悬浮框(不同模型悬浮框独立)显示加载进度或下载进度(下载要先有确定框),也就是"正在验证XXX模型(-->正在下载XXX模型-->下载XXX完成)-->XXX验证完成".
    - 已废弃的本地对话模型：仅当用户未配置大模型并触发 AI 功能时才验证加载；该路径后来已删除。
    - embedding模型: 软件启动后立即加载.
    - rerank模型: 软件启动后立即加载.
    - paddleocr模型: 当用户启用了OCR的时候,软件启动后立即加载;或者用户从未启用到启用OCR之后立即加载.

- [x] 后端代码大重构带了了莫名其妙的bug,并且深刻怀疑其根源相同:
  - [x] 修复搜索框搜不到结果的bug
  - [x] 修复文件树不显示任何文件的bug
  - [x] 修复文件点击创建没反应的bug

## 移动端悬浮侧栏

- 顶栏触发的浏览器、Git、Todo、Agent 以及编辑器右栏在移动布局下统一改为悬浮侧栏；文件树、搜索只读预览与这些右栏按打开顺序只保留最新一项。
- 搜索页单击文件改为复用智能表格同款全局编辑器侧栏，统一支持 PDF、Word、图片等文件管线；移动布局下悬浮，桌面端保持停靠。
- 移动断点改为依据不受侧栏宽度影响的工作区总宽度，消除侧栏开关引发的布局反复切换、抽拉闪烁和崩溃。
- 定向 Vitest 共 6 项通过；Chromium 完整移动交互及截图验证通过，移动布局翻转计数为 0；桌面浏览器侧栏拖拽回归通过。

## 默认界面与正文字体

- 将用户提供的 `HYWenHei-85W.otf` 纳入前端字体资源，通过 `@font-face` 注册并由 Vite 随桌面前端产物打包。
- 界面与正文默认字体栈均以 `HYWenHei-85W` 为首选，保留 Google Sans、Noto Sans SC 与系统字体作为 fallback；用户显式字体设置仍可覆盖默认值。
- 字体资源单测、Vite 生产构建及 Chromium 实际加载/渲染冒烟测试通过；全项目类型检查仍受本次改动之外的既有 TypeScript 错误阻塞。
- [x] Agent上传图片有bug,无限阻塞在上传页面.即使要解析也应该在自己的文件上传块处显示解析进度条,而不是阻塞整个页面或者阻塞用户上传文件.
  - 应该在基础设置的 OCR 下面新增“识图”，默认关闭；关闭时 Agent 识图工具仅使用 OCR，不进入当时的本地视觉链路，避免无限等待或返回错误。
  - 同理，识图没有开启时，Agent 调用工具不得自动加载当时的内置本地模型。
- [x] 修复存储管理中模型failed to fetch的bug
- [x] 工作区大卡片响应式布局优化: 主要测试手段是打开联网搜索侧边栏然后拽住左边拖动,此时工作区大卡片就会左右坍缩,根据当前宽度实现响应式布局:

## 文件库、图书馆、组件库、文献库联合搜索

- 新增 `/search` REST 与 `SearchAllLibraries` gRPC，对文件库、图书馆、组件库、文献库执行同一领域用例；来源参数严格门控实际后端调用，标题始终搜索，全文和语义只按用户开关执行。
- 文件全文合并解析索引和磁盘文本；图书馆本地书籍复用真实文件正文/向量命中，网页条目只搜索已持久化元数据；组件搜索完整 Vue/HTML 源码；文献搜索完整智能表格文本单元格。
- 非文件类语义内容以 `unified_search_chunk` 写入长期记忆和 Chroma，使用 `user::knowledge::active_library` 隔离，按内容哈希增量更新并清理已删除来源；语义结果继续经过向量召回、关键词候选合并与 ReRank，不使用部分匹配伪装。
- 搜索下拉菜单保留内容与语义开关并新增四个可多选库标签；统一样式按后端相关度混排并在右上角标明来源，分裂样式纵向复用文件中图标、图书卡片、组件卡片和文献文件块。
- 新增后端 service/REST/gRPC、前端 API/页面/筛选及响应式 Playwright 定向测试；通过 Vite 开发代理对真实 `/search` 请求返回 200，并完成桌面、平板、移动、超窄两种样式截图检查。
