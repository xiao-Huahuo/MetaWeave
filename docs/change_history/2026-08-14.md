# 2026-08-14

- [x] 每个新对话代表一个独立对话线程,创建新对话的时候左侧历史边栏的旧对话应该显示转圈,直到输出结束. 用户可以独立的并行进行多个对话而不被打断.
- [x] 根治 TODO 侧边栏删除后自动化仍到点触发的问题：普通 TODO、自动化定义与运行记录改为同一事务级联删除；调度扫描会自动清理历史孤儿任务和崩溃遗留的 `running` 记录。
- [x] 加固调度并发与长任务执行：只按空闲 worker 数抢占任务，租约令牌贯穿执行前校验、同步续租、运行心跳和完成写回；删除、停用、租约过期或被新 worker 接管后，旧执行会被取消且结果不会推进计划。错过的循环周期直接跳到首个未来时间，避免密集补跑。
- [x] 统一 REST、gRPC 和 Agent 内置工具的自动化生命周期入口：新增严格请求 DTO 与 5 个 Automation RPC，字符串布尔值不再被误判；默认自动化改为单次执行，降低误建长期任务的风险。
- [x] 重做 TODO 侧边栏自动化交互：自动化不再使用普通完成复选框、隐藏已完成、清除完成和伪日期编辑；新增启用/暂停/完成/失败状态、时区与最近结果、暂停/恢复、逐项 pending、失败回滚和二次删除确认，创建表单增加未来时间校验与完全访问权限警告。
- [x] 新增服务、调度器、事务回滚、REST/gRPC、Agent 工具、前端 API/store/component 和 Playwright 浏览器流程测试，覆盖暂停→恢复→删除→刷新不回现及普通 TODO 回归。
- [x] 热力图按当前卡片宽度响应式显示最近 1～12 个月：保留固定 12px 方块与 4px 间距，通过 ResizeObserver 裁剪可见周列，不缩放、不产生横向或纵向滚动。
- [x] 修正热力图响应式容器：月份刻度与周列现在依据规划区真实可用宽度计算，不再被内容本身撑宽后裁切；总览筛选改用绿色体系。
- [x] 总览热力图改用明确的贡献图绿色阶梯：零值为浅灰，活跃值按六级从浅绿过渡到深绿；其他模块筛选仍保留各自色系。
- [x] 热力图七项筛选改为复用 AgenticRAG 三率图左上角的胶囊 Toggle：动态测量当前按钮宽度，滑块按按钮位置平滑移动，并监听容器尺寸变化。
- [x] 调整 Dashboard 规划区列比例，压缩左侧统计列并扩大热力图列，避免宽屏下热力图被限制在窄列而只能显示少量月份。
- [x] 修复热力图右侧容器未参与伸展的问题：规划区主内容现在填满自身网格列，消除热力图与耗时图之间的异常空洞并让 ResizeObserver 获得完整可用宽度。
- [x] 热力图增加顶部月份刻度，格子继续通过日期标题提供悬停日期；卡片高度改为内容自适应，并将规划区剩余留白置于热力图上方。
- [x] 热力图改为直接复用 Origin UI Vue 官方 `ContributionGraph / Group / Block` 组件结构：删除自制网格、热力图内部右侧日期总览栏和内部滚动；保留 Dashboard 原有右侧耗时图，恢复示例的 364 天、52×7 分组以及统一 12px 方块和 4px 间距。
- [x] 修复首次加载历史活动超过通用 30 秒请求时限后热力图显示“活跃数据加载失败”的问题；活动汇总接口使用独立的 120 秒首次回填窗口，其他 API 超时策略保持不变。
- [x] 新增 Dashboard「每日活跃热力图」：使用 SQLite 活跃事件表持久化有效操作，按图书馆、文档、知识、Agent、任务与其他模块执行动作计分、日上限和连续编辑去重；REST/gRPC 统一提供年度聚合数据，并从现有 Agent、图书馆、收藏、智能表格、任务队列、TODO 与密码库记录安全回填。前端在 Dashboard 中间区域加入 Origin UI 364 日热力网格、七类独立色阶筛选和四项密集统计，并支持 reduced-motion。
- [x] 统一工作区菜单系统：智能表格标签/星级筛选、密码库与 Markdown 右键菜单、Agent Skill/思考模式/历史操作、任务队列并行数及看板图表筛选统一复用 Reka 下拉组件或同款指针菜单表面，并补充一致的主题背景、开合动效、键盘语义与 reduced-motion 处理。

- 图书馆筛选菜单接入共享 Reka UI DropdownMenu：按类型与标签分组展示单选项，保留标签分页，并统一弹层、选中态、键盘导航与焦点管理；修复 Portal/Primitive 最终节点无法命中 scoped 样式而丢失背景、间距和动效的问题。
- 修复任务队列内嵌 Agent 页面反复进入历史加载态的问题：继续复用完整 `AgentPanel`，首次打开正常加载固定会话；处理中任务通过 ChatStore 静默同步持久化消息并保留现有消息对象，等待确认后停止同步；关闭弹窗时清理定时器。
- 移除观察节点按固定 5 次继续观察或 18 个工具结果强制回答的硬编码收敛逻辑；观察模型返回 `continue` 时继续探索，仅保留既有上下文溢出压缩机制。
- 修复 Windows Electron 最大化窗口从顶栏拖动时无法恢复原窗口尺寸和圆角的问题：仅在最大化期间临时启用原生 resize，并在 `unmaximize` 后立即恢复无系统描边的不可调整状态。
