# 2026-09-09

## 扫描器空白图片卡住修复

- DOCX、PDF 与普通图片共用的结构化 OCR 入口现在会在加载模型前识别纯色或全透明空图，直接返回明确的“无文字”结果。
- 既有 `ocr.timeout_seconds` 配置现在实际约束单图推理；第三方流水线不返回时，扫描任务不再永久占用唯一 worker，超时后的迟到进度也不会回写终态任务。
- 新增空图短路、阻塞推理超时、DOCX/PDF 扫描终态和 Chromium 上传到结果页回归。

## 扫描器 DOCX No OCR 图片预览修复

- DOCX 结构化投影现在把文档流中的图片关系占位符替换为已解包资源的 Markdown 图片节点，并保持图片在原文中的顺序；已内联图片不会再在文末重复追加。
- 新增扫描器服务回归和 Chromium 页面冒烟：关闭 OCR 后不再出现 `DOCX 图片引用`，图片请求成功且浏览器自然尺寸非零。

## 扫描器 DOCX 图片预览修复

- DOCX 的 No OCR Markdown 投影现在将文档流中的图片关系占位符替换为已解包资源的真实图片节点，并保留图片原始位置；已经内联的图片不再于文末重复追加。
- 新增扫描器服务回归和 Chromium 页面冒烟断言，覆盖占位符消失、资源文件落盘、图片请求成功及浏览器自然尺寸非零。

## PDF 表格 Markdown 结构修复

- PDF 文本层解析现在把 PyMuPDF 检出的表格输出为合法 GitHub Flavored Markdown 表格，补齐表头分隔行、外围管道、列数对齐以及单元格换行和竖线转义。
- 检出表格后会从普通页面正文中排除位于表格区域的文本块，避免同一批单元格先纵向展开、再以表格重复出现。
- 使用 `2024计算机科学与技术.pdf` 新增回归测试；PDF、多模态清洗与扫描器服务共 31 项定向测试通过，生产前端构建通过。真实 Chromium 上传验收得到 4 张表格、第一页 6 列表头，截图保存于 `docs/acceptance/pdf-table-real-smoke.png`。

## XLSX、XLS 与 DOCX 表格 Markdown 修复

- CSV/TSV、XLSX、XLS 和 DOCX 共用的表格格式器改为合法 GitHub Flavored Markdown，补齐表头分隔行、外围管道、列宽补齐、换行与竖线转义。
- XLSX 按单元格坐标保留未序列化的空列；DOCX 按 `w:gridSpan` 保留横向合并单元格的实际列数，避免后续单元格错列。
- 新增旧版二进制 XLS 的 `xlrd` 读取路径，并将 `.xls` 纳入服务级支持后缀；扩展名为 `.xls` 但内容为 OOXML 的文件仍复用 XLSX 解析器。
- 多模态回归 30 项通过，知识预览/扫描器相关回归 21 项通过，生产前端构建通过。Chromium 真实上传验收：XLSX 生成 1 张表格（首表 13 列）、XLS 生成 3 张表格（首表 6 列）、DOCX 生成 18 张表格（首表 2 列）。

## 扫描器与灌库进度细化

- 扫描器与单文件灌库任务的持久化进度由整数升级为一位小数，新增 Alembic `20260909_0012` 同步迁移两个进度字段；清洗、扫描器映射和入库批次计算不再提前截断小数。
- 结构化 OCR 向扫描器发布“加载模型、分析版面/文字/表格/公式、整理结果”三个真实检查点。PaddleX 单次推理内部没有可信百分比回调，因此没有加入定时增长或伪造子进度。
- 扫描器中央进度、灌库/图谱队列与顶栏进度统一通过共享格式化函数显示一位小数；进度条宽度仍直接使用后端值。
- 后端相关回归 49 项通过；前端组件与 API 定向测试 14 项通过；生产构建和 Chromium 冒烟通过。1024、768、480 三档均验证扫描器、灌库页和顶栏的阶段文案、百分比与组件横向溢出。
- 进一步将单图 OCR 从单一黑盒检查点改为模型裁决进度：观测 PaddleX 六个顶层顺序节点的真实开始/返回边界，以“已裁决节点 / 6”推进；公式、表格无命中时明确记为跳过。真实表格图片得到 `0 → 16.7 → 33.3 → 50.0 → 66.7 → 83.3 → 100.0`，公式分支跳过、表格分支完成，最终仍正确输出表格。
- 将最耗时的 GeneralOCR 继续细分为文字检测、文字行方向和文字识别，形成 8 个裁决节点。真实表格图片检测到 35 个文字框，方向与识别均持续报告 `n/35`，OCR 内部进度从 62.5% 逐步推进至 87.5%，不再在文字阶段固定不动；真实推理无原生崩溃且表格结果保持正确。

## Preview 结构块联动

- 新增 scanner OCR 块持久化和 Alembic `20260909_0013`，保存 `page:block_id`、类型、内容、阅读顺序、页面尺寸与 bbox；旧扫描记录兼容为空块列表。
- 图片与 PDF Preview 使用内容同层 SVG 框，滚动和缩放直接复用内容变换；Markdown Preview 将 OCR 内容按顺序匹配到 Vditor 实际 DOM。左右悬浮同步高亮，点击锁定并将另一侧滚动到对应块。
- 块数据只传给 Preview：右侧 Edit 不渲染，Split 只在 Preview 半侧渲染；DOCX 左侧明确不显示空间框。
- 将 Vditor 中文资源和 Lute 运行时静态打入生产前端，解决 CSP 阻止 `unpkg.com` 动态脚本后 Preview 为空的问题，无需放宽 CSP 或依赖外网。
- 后端及迁移定向测试 11 项、前端组件测试 9 项、生产构建和 Chromium 联动冒烟通过；1024、768、480 三档截图完成，并验证缩放前后框的归一化位置与宽度不变。

## 高质量 PP-StructureV3 结构化 OCR 流水线

- 将基础 PP-OCRv5 Mobile 文字识别替换为统一的 PP-StructureV3 高质量流水线：`PP-DocLayout-L`、`PP-DocBlockLayout`、PP-OCRv5 Server 检测/识别、文档与文本行方向、UVDoc、表格分类、SLANeXt 有线/无线表格、单元格检测和 `PP-FormulaNet_plus-M`。图表解析和印章识别保持关闭。
- `ImageOcrService` 现在串行返回结构化 Markdown、分页版面块、坐标、阅读顺序、文字置信度、表格/公式数量和实际版面标签；扫描器、知识灌库、会话附件和网页图片继续复用同一入口。
- 图片与扫描 PDF 的结构化 metadata 写入 frontmatter；扫描器 OCR 草稿保留标题、表格 HTML 和公式 LaTeX，同时移除图片节点。全局推理锁阻止多个业务入口同时执行流水线，删除模型时先释放共享模型引用。
- PaddleOCR 模型下载改为受管模型组：全部唯一组件同步到 `runtime/models/paddleocr/<model_name>/`，marker 必须同时匹配流水线版本、模型清单和每个 `inference.yml`。旧 Mobile marker 会被判为不完整并请求用户确认重新下载。
- 存储管理将原 PaddleOCR 项更新为“PaddleOCR 结构化流水线”，展开后显示版面、文字、表格、公式、预处理和关闭模块；存储路径树同步更新名称。
- 固定 `paddlepaddle==3.3.1`、`paddleocr[doc-parser]==3.7.0` 与 `paddlex[ocr]==3.7.2`。补齐 PP-StructureV3 实际创建时强制检查的 PaddleX OCR extra；PyInstaller 收集 PaddleX 推理流水线、模型子模块和 YAML 配置，不把模型权重放入 EXE。
- 修复 `build_frontmatter_file()` 主体误落在相邻方法 `return` 之后的问题，恢复图片/文本附件与单文件 frontmatter 的正式解析路径。
- 在 `docs/WORKFLOW.md` 增加完整 OCR 流程图和结果/存储合同，在 README 的扫描器/OCR 与模型管理章节同步当前行为。
- 修复正式 EXE 未继承终端 PATH 时误报 MiKTeX 未安装的问题：LaTeX 服务除托管目录与 PATH 外，还会读取 Windows MiKTeX 注册安装根目录及固定用户安装位置。下载失败时模型管理保留后端原始异常，不再只留下通用失败提示。

## 验证

- 结构化 OCR、模型管理、LaTeX 探测、扫描器与打包配置定向回归：37 项通过，2 项按原有 PATH 条件跳过；新增测试真实检查 `paddlex` 的 OCR extra 可导入，并覆盖空 PATH 下的 MiKTeX 安装根目录发现及 OCR 原始错误进度。
- PaddleOCR 下载与环境覆盖定向测试：2 项通过；前端模型管理组件：6 项通过；模型管理 API 客户端：2 项通过。
- 相关 Python 文件通过 `compileall`；相关前端文件通过定向 ESLint；`npm run build-only` 通过。
- `pytest tests -q` 的首次全量结果为 584 通过、2 跳过、24 失败；其中本次新增 OCR 配置缺少逐字段用途说明、旧 PyInstaller hidden-import 断言两项已修复并定向复验通过。其余失败属于既有数据库迁移、Agent/RAG、外观/反链和知识库点目录用例，不在本次 OCR/MiKTeX 修改路径内。直接运行无目录约束的 `pytest -q` 还会误收集 `runtime/trash` 中的用户测试文件，因其缺少同目录模块在收集阶段中止。
- Chromium 连接隔离启动的真实前后端完成存储管理冒烟：PaddleOCR 显示“使用中 / 已启用 / 2.0 GB”，MiKTeX 显示“可用 / 系统安装 / 982.5 MB”，且没有“安装 MiKTeX”操作。另保留 1024、768、480 三档结构详情响应式检查。
- 全量 `npm run type-check` 仍被既有 ImagePreviewer、SmartForms、CarouselBlock 等错误阻断，本次修改文件没有出现在错误清单中；当前 Python 环境未安装 ruff，因此未执行 ruff 检查。
- 额外的既有 `ModelLifecycleOverlay.spec.ts` 仍按旧展开通知行为断言，和组件当前默认 compact 行为不一致而失败；本次未修改该组件。
- 已实际下载缺失的 `PP-DocLayout-L`、`SLANeXt_wireless` 与 `PP-FormulaNet_plus-M`，并将全部 13 个唯一组件同步到 `runtime/models/paddleocr`（2,124,438,217 字节）。从该受管目录加载 PP-StructureV3 后，对仓库表格图片完成真实 CPU 推理；另用现有 MiKTeX 编译并渲染公式与表格混合页，真实识别得到 1 页、1 张表格、1 个公式、5 个版面块，公式恢复为 LaTeX、表格恢复为 HTML。

## 扫描器有界并发队列与单任务硬终止

- 将扫描器后台执行从不可单独取消的线程池改为 SQLite 持久队列、单调度线程和独立 `spawn` 工作进程；`AgentConfig.BusinessLimitsConfig.scanner_worker_count` 默认值调整为 2，并继续支持 `AGENT_LIMIT_SCANNER_WORKER_COUNT` 服务级覆盖。资源满时任务保持 `queued`，进程退出后自动领取下一项。
- 每个运行任务由 `ScannerService` 持有唯一进程句柄。新增 `POST /scanner/{scan_id}/cancel`：等待任务直接进入 `cancelled`，运行任务先 `terminate`、超时后 `kill`，只释放目标任务并立即补位，不影响其他扫描。应用关闭时运行任务回到队列，重启后继续调度；已有 `multiprocessing.freeze_support()` 覆盖打包运行时。
- 左侧最近解析块右侧新增红色方形终止按钮，真实接通取消 API；排队中、解析中、正在终止、已终止分别显示明确状态。取消无需确认弹窗，点击后立即执行。
- 验证：`tests/test_scanner_service.py` 7 项通过（含并发槽位、排队及真实子进程终止）；扫描器 API/组件 Vitest 12 项通过；Chromium 扫描器界面冒烟 3 项通过，并在 1024/768/480 三档完成无溢出与视觉复审，截图为 `docs/screenshots/scanner/queue-running-*.png`。取消接口经 Vite 5173 开发代理真实请求得到后端预期 404，验证代理链可达；测试端口均已关闭。

### Windows OCR 原生访问冲突修复

- 修复部分 DOCX 内嵌 JPEG 直接送入 Paddle/PaddleX 时导致扫描工作进程以 `3221225477 / 0xC0000005` 退出的问题：进程隔离的 OCR 路径先由 Pillow 应用 EXIF 方向、合成透明通道并写成临时标准 RGB PNG，再调用原生模型；临时输入在同步推理结束后自动清理，No-OCR 原图与导出内容不变。
- 新增独立 OCR 资源槽位 `scanner_ocr_worker_count=1`。扫描器总进程并发仍为 2，普通及 No-OCR 任务可以并行；OCR 任务按模型容量等待，避免两个进程同时重复加载约 2GB 模型。
- 使用数据库中此前稳定复现崩溃的同一 DOCX、同一 spawn 工作进程和同一受管模型真实回归，任务从原失败点 63.8% 继续到 100% finished，验收记录随后删除。串行回归：scanner 9 项、structured OCR 9 项、multimodal cleaner 30 项全部通过。
