# 2026-09-08

## 修复开发脚本与正式包启动竞态

- 开发启动脚本在后端返回正确 `/health` 后才启动前端，并统一传递后端、Vite、开发代理和 Electron 使用的端口地址；端口清理失败会中止启动，不再静默继续。
- Electron 将 TCP 端口探测改为有超时、可取消的 HTTP 就绪探测；Vite 冷启动最长等待 120 秒，旧托盘实例收到二次启动时会使用新实例传来的开发地址重新加载。
- 正式安装包启动时校验 MetaWeave 健康响应，拒绝占用 8002 的无关服务；内置后端在启动期间报错或退出会立即进入失败页，不再空等完整超时。
- 新增真实 loopback HTTP 与受管进程退出回归测试；按要求不重新构建 PyInstaller EXE 或 Electron 安装包。

验证：

- `serverReadiness.spec.ts`：4 项通过，覆盖短暂失败恢复、错误服务占端口、正式包健康成功和托管后端提前退出。
- `windowContentLoader.spec.ts`：6 项通过，覆盖旧单实例窗口恢复并重新加载。
- Electron 三个 CJS 文件通过 `node --check`，相关 CJS/TS 文件通过定向 ESLint，新增测试通过定向 TypeScript 检查。
- `VITE_PORT=15173` 实际启动成功并在 616 ms 就绪，页面可加载且 15173 已在验证后关闭；后端不可用时健康检查 CLI 正确返回失败。
- `npm run build-only` 通过；未执行 PyInstaller 或 Electron Builder。全量 `npm run type-check` 仍被本次修改前已有的 ImagePreviewer、SmartForms、CarouselBlock 等错误阻断，本次新增文件不再出现在错误清单中。

## 新增扫描器工作区

- 新增持久化扫描记录、Alembic 迁移、受控后台解析任务与 `/scanner` REST API。上传文件统一隔离到当前知识库 `.mw/scan/<scan_id>/`，任务状态、OCR/no OCR 可编辑草稿和收藏均由 SQLite 持久化。
- 复用现有多模态灌库链的 Markdown 投影阶段，在 embedding 前停止；OCR 开启时生成纯文字 OCR 结果和保留本地图片资产的 no OCR 结果，网页入口包含公共地址校验、重定向校验、大小限制及 HTML-to-Markdown 转换。
- 新增 MinerU 式扫描器页面、最近解析栏、拖放/系统文件选择、网页链接、OCR 设置、像素进度、7 个真实示例、双栏原文件/Markdown 编辑区、知识库保存、系统另存为导出和全文复制。
- 收藏页新增“扫描器”子页，复用扫描历史卡片；删除历史同步删除收藏记录与隔离的受管文件。
- Electron 新增受限的系统另存为桥接；Vite 新增 `/scanner` 开发代理；存储管理登记 `.mw/scan`。

## 扫描器界面一致性与响应式重构

- 扫描器改为 Agent 下方的一级活动栏入口并使用扫描语义图标；历史侧栏统一为 Agent 历史抽屉的尺寸、位置、圆角、无边框外观和折叠动效，补充标题、镜像折叠/展开入口、记录瀑布入场和居中空态。
- 修正“上传解析”按钮误用主题红色强调态和未注册图标回退为扳手的问题；悬停状态改用扫描器主色，图标改为上传文件语义。
- 抽取 `EditorPaneToolbar` 供正式编辑器与扫描器双栏共同复用，统一模式名称为中文，并共享文件标签、模式切换和保存按钮动效；扫描器使用同组件的紧凑布局。
- OCR/No OCR toggle 移除子按钮椭圆悬停底色；上传解析按钮与历史块等宽，折叠态展开按钮与返回按钮纵向对齐，双编辑区分隔线支持 25%–75% 范围拖动。
- 上传区支持整面点击文件选择器，使用无滚动的强响应式网格；示例按容器宽度整页循环轮播，卡片悬停仅改变边框与常规阴影，图片层级固定在压缩后的信息区下方，标签对齐图书馆样式。
- 网页链接表单复用图书馆表单表面与输入焦点动效；解析结果顶栏对齐图书馆 44px 高度，OCR 切换复用资源管理器 segmented toggle 样式，左右编辑区统一复用 `EditorModeSwitch`，并仅保留两区之间的分界线。
- 移动端跨入 820px 断点时自动收起历史侧栏，为左上展开按钮保留独立槽位，避免覆盖上传标题、返回按钮和解析内容。

## 验证

- `python -m pytest tests/test_scanner_service.py -q`：通过。
- `npx vitest run src/api/__tests__/scanner.spec.ts`：通过。
- `npm run build-only`：通过。
- `npx playwright test e2e/scanner.spec.ts --project=chromium --workers=1 --max-failures=1 --retries=0`：通过；已检查 1024、768、480、明暗主题、解析中、结果页与收藏子页截图。
- 经 Vite `5173` 开发代理真实请求 `/scanner/files` 返回 200，任务完成后测试记录及 `.mw/scan` 受管文件已通过删除 API 清理。
- 全量 `npm run type-check` 仍被本次修改前已存在的 ImagePreviewer、SmartForms、CarouselBlock 等 TypeScript 错误阻断；扫描器新增文件未出现在错误清单中。
- `npx vitest --run src/components/scanner_view/__tests__/ScannerExperience.spec.ts`：6 项界面复用与交互契约通过。
- `npm run build-only`：扫描器界面重构后通过。
- `npx playwright test e2e/scanner.spec.ts --project=chromium --workers=1 --max-failures=1 --retries=0 --reporter=line`：通过；实际验证整区文件选择、URL 表单、侧栏折叠、响应式轮播、历史动画、上传到解析结果、OCR 切换、双编辑区和收藏页，并更新 1024/768/480 截图。
- 顶栏与拖动补充验证：3 个定向 Vitest 文件共 10 项通过；扫描器 Chromium 流程通过并实际验证 toggle 悬停、按钮等宽、按钮对齐及分隔线拖动。

- [x] 新增页面: 扫描器
  - 整体模仿MinerU的界面布局:
    - 左侧栏: 上方按钮:上传解析(打开系统文件),下面是一个一个"最近解析"块,块的外观和文件树的"最近浏览"块相似.标题下显示大小,左下角显示上传的时间,右下角不显示两状态,而是显示3个图标按钮:打开原文件所在位置,收藏,删除历史.点击则恢复此文件的解析结果页面.
    - 原页面:
      - 主区域: 一块巨大的文件上传区域,可拖拽,里面有一个无底图标png,下面2个按钮: 上传文件(打开系统文件,选择任何文件),网页链接(输入网页链接后使用爬虫爬下来,并作为markdown文档).区内右上角有个设置按钮,点击后下拉菜单包括: OCR(打开则启用ocr,默认开启).所有的下拉菜单应该复用文件树右键菜单的样式和动效.
      - 主区域下方: "示例",每一个示例块里面有一个图片,阴影悬浮,悬停时微旋转并可以超出示例块边界之外,上2/3是承载图片的区域,下1/3显示内容标题和tag.点击示例块可以直接开始解析此示例图片.
    - 解析过程中,主上传区的所有内容消失,中央显示一个像素loader,其下显示解析解析中,下面显示进度条,左侧栏显示此文件,其图标外要有一个转圈loader,标题下显示解析中.
    - 解析的本质是复用项目已有的"灌库"链,得到中间结果(markdown层),上传的文件应该放到.mw/scan里面.
    - 解析结果页:
      - 左侧显示上传的文件的编辑区(复用组件).
      - 右侧也是一个编辑区,但是显示此文件的解析结果markdown,且可编辑,
      - 顶栏显示一个回退按钮和文件标题,标题右边有一个复制(图标按钮),顶栏右侧的图标按钮依次是: 打开文件夹,收藏,保存(导出markdown到知识库内,文件名默认采用原文件名.md),导出(导出markdown到外部),复制(全文复制).
      - 解析时的ocr:
        - 若启用ocr,则右侧编辑区左上角显示一个toggle: ocr/no ocr,ocr的显示的markdown中全文皆文字,不显示图片.保存和导出时均为md文件;no ocr的则显示的是下面所说的,保存和导出也是下面所说的.不开启ocr则没这个toggle.toggle样式和动效复用文件资源管理器左上角的.
        - 若不启用ocr,则右侧编辑区的markdown的preview可显示图片.保存时则为md文件+文件同级目录的.assets/(默认)里面的图片(依照用户的基础设置里面的markdown图片目录).导出时则导出md+.assets/(默认)压缩为压缩包的zip.
  - 收藏页面新增扫描器子页: 同样的,只显示收藏的扫描器历史,尽量复用组件.
