# 2026-09-03 变更记录

## Agent 全工具可用性审计

- 从正式运行时注册表确认 107 个原生 Agent 工具，并新增低内存逐工具成功路径测试；与既有扩展和附件测试合并后覆盖 107/107。
- 修正 MCP 注册测试仍断言已移除示例工具 `echo_text` 的问题，改为验证正式工具 `list_available_tools`。
- 更新 Agent 工具预览浏览器测试，使用当前正式工具 `list_available_tools`，移除已无产品实现对应的流式 DOM 断言，并按现有 `::after` shimmer 实现校验。
- 后端各测试文件严格串行通过；前端生产构建通过；Chromium Tool/Chat 两种实际界面冒烟通过。
- 初始单元审计阶段未调用模型 API；后续纠正验收按用户授权临时配置模型，完成后已清空凭据。

## Agent 全工具真实 API 纠正验收

- 使用真实 FastAPI lifespan、DeepSeek 模型和 `/agent/stream` SSE 对 107 个工具逐项验收；首次通过即落账跳过，最终 107/107 通过。
- 修复工具线程读取 REST ContextVar 导致的 503，改为应用 Service 显式注入 ToolRuntimeState。
- 修复无代理时联网工具被硬拒绝，代理改为可选并完成真实网页/图片搜索。
- 修复 DSH 内置依赖闭包、Windows 长路径解压、失败状态掩盖、租约泄漏和子 Agent 冷恢复；spawn/continue/wait 均由真实 API 通过。
- 单独完成当时的内置本地识图 API 验收，避免与其他高内存任务并行。
- 审计结束后清空临时模型凭据、关闭审计开关并删除 120 个临时会话。
