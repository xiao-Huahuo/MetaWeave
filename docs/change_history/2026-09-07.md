# 2026-09-07

## 组件库大文件支持

- 将组件源码服务层上限从 250,000 个字符提高到 2,000,000 个字符，与 REST/gRPC DTO 边界保持一致。
- 对超过 1,000,000 个字符的源码关闭自动 iframe 编译，源码仍可完整上传、保存、读取、编辑和复制，避免超大预览阻塞界面。
- 增加服务层、前端 API、预览组件和真实 Chromium 上传验收，覆盖单文件同时超过 1 MiB 与 100 万字符的场景。

## 联网配置整合

- 将用户设置中的“浏览器”独立页签并入“联网配置”，在同一页面连续展示联网搜索与内置浏览器配置，并保留各自原有保存逻辑。
- 兼容历史保存的 `browser` 活动页签并自动迁移到 `web`；补充 390px 窄屏适配与 Chromium 实际界面回归。

## 低成本图谱抽取设计

- README 将语义图谱抽取更新为最终版“本地优先、章节增量、联网裁决”机制，详细说明缓存、候选分流、失败降级、分层去重与原子落库。
- `docs/WORKFLOW.md` 的图谱抽取流程图同步改为未变化内容零抽取、变化章节本地处理、联网模型仅裁决最小灰区证据的目标流程。
- 完成当时的低成本图谱抽取实现：统一使用 `ingestion_hash`，新增章节结果与实体对判定缓存，全文仅进入内置本地模型或确定性规则，联网小模型只接收白名单化灰区候选和最短证据；抽取与全库去重均按本地高低阈值分流，失败候选可独立重试。该链路后来已替换为远程小模型全文抽取与确定性规则兜底。
- 图谱替换改为旧图删除、新图写入、章节缓存和文档状态同一 SQLite 事务提交；新增 Alembic `20260907_0010`，并覆盖重复抽取、局部章节更新、联网失败、去重缓存、事务回滚和迁移回环测试。

## 知识文件回收站派生数据一致性

- 删除知识文件或文件夹时同步清理 `.mw/md`、`.mw/frontmatter`、`.mw/assets`、向量切片和对应图谱，避免残留孤儿资源。
- 从回收站恢复源内容后自动复用正式灌库与图谱抽取链路重建派生数据，并向前端返回重建状态；重建失败时界面给出明确提示。
