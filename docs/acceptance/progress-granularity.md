# 进度粒度验收

- 扫描器：后端 `12.5%` 的投影进度映射后保留为 `9.5%`，不再被截断为 `9%`；UI 将 `55.26` 显示为 `55.3%`。
- OCR 状态：依次发布“正在加载结构化 OCR 模型”“正在分析版面、文字、表格与公式”“正在整理结构化 OCR 结果”。
- 灌库：数据库与服务保留 `46.27 → 46.3`，队列页将 `64.26` 显示为 `64.3%`。
- 顶栏：入库与图谱进度统一保留一位小数，`45.26 → 45.3%`、完成态显示 `100.0%`。
- 视觉：Chromium 在 1024、768、480 三档完成扫描器与灌库页截图；相关组件无横向溢出，窄屏文本按现有优先级省略，百分比完整可见。
