# OCR 模型裁决进度验收

- 观测边界：PaddleX 3.7.2 的文档预处理、版面检测、区域检测、公式裁决、文字检测、文字行方向、文字识别、表格裁决八个节点。
- 进度规则：节点开始只更新当前状态；节点返回后才结算 `1/6`；未命中的公式/表格分支计为裁决跳过；最终进度单调且为 100%。
- 真实表格图片结果：文字检测得到 35 框；方向和识别均按 `n/35` 推进，内部进度覆盖 62.5%–87.5%；公式裁决跳过，表格裁决完成，输出仍包含 1 张结构化表格。
- 稳定性：底层 Predictor 代理方案在 Windows 原生推理中触发访问冲突，已完全撤回；正式实现只代理顶层 Python 流水线对象，并在异常和成功路径恢复原对象。
- UI：扫描器显示当前节点和“已裁决 n/6”，一位小数进度与进度条同步；1024、768、480 三档 Chromium 冒烟通过。
